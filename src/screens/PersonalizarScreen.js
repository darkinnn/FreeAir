import { View, Text } from 'react-native';

const PersonalizarScreen = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{fontSize: 20}}>PersonalizarScreen</Text>
    </View>
  )
}

export default PersonalizarScreen;